<!-- フォローステータス-->
 <% @user ||= current_user %>
<div class="stats">
  <a href="<%= following_user_path(@user) %>" class="link">
    <strong id="following" class="stat">
      <%= @user.followings.count %>
    </strong>
    フォロー中
  </a>
  <a>   </a>
  <a href="<%= followers_user_path(@user) %>" class="link">
    <strong id="followers" class="stat">
      <%= @user.followers.count %>
    </strong>
    フォロワー
  </a>
</div>